import { PriceFormatDirective } from './price-format.directive';

describe('PriceFormatDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceFormatDirective();
    expect(directive).toBeTruthy();
  });
});
