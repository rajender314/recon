import { AllowNumbersDirective } from './allow-numbers.directive';

describe('AllowNumbersDirective', () => {
  it('should create an instance', () => {
    const directive = new AllowNumbersDirective();
    expect(directive).toBeTruthy();
  });
});
