import { DomSearchPipe } from './dom-search.pipe';

describe('DomSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new DomSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
